info:
  name: Add friend
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/friends"
  body:
    type: json
    data: |-
      {
        "username": "jorge@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
