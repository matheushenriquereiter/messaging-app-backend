info:
  name: Get friends
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/friends"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
