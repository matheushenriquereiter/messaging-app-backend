info:
  name: Send friend request
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/friends/requests"
  body:
    type: json
    data: |-
      {
        "username": "jorge"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
