info:
  name: Friend sign up
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/auth/sign-up"
  body:
    type: json
    data: |-
      {
        "username": "jorge",
        "email": "jorge@gmail.com",
        "password": "jorge123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
