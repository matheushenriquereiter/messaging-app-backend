info:
  name: Friend sign in
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/auth/sign-in"
  body:
    type: json
    data: |-
      {
        "email": "jorge@gmail.com",
        "password": "jorge123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
