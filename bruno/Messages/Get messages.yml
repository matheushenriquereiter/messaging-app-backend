info:
  name: Get messages
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/messages"
  body:
    type: json
    data: |-
      {
        "username": "Jorge"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
